# Restarts the container cc-busy-A (Precondition: the container must be created and started)
# docker container restart --time 5 cc-busy-A: restarts the container cc-busy-A after 5 seconds
docker container restart cc-busy-A
